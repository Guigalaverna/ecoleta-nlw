const uploadButton = document.querySelector('button[name=image]')
const uploadLabel = document.querySelector('label#upload-label')
const inputFile = document.querySelector('input#upload-image')

uploadLabel.addEventListener('click', () => {
    
})
